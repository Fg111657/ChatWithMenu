# Frontend Environment Variables
# Copy this file to .env and replace with real values

# Supabase Configuration (FRONTEND ONLY - Anon Key)
REACT_APP_SUPABASE_URL=https://YOUR-PROJECT-ID.supabase.co
REACT_APP_SUPABASE_ANON_KEY=YOUR_ANON_KEY_HERE

# ⚠️ NEVER put the service role key in frontend
# ⚠️ Replace placeholder values with real keys from Supabase dashboard
# ⚠️ .env is already in .gitignore - never commit it
